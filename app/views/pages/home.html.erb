<div class="container">
  <div class="search text-center">
    <%= form_tag bases_path, :method => "get" do %>
      <%= text_field_tag :search, params[:search],
      placeholder: "Procure por um termo" %>
      <%= submit_tag("Buscar", :name => nil, class: "btn btn-warning") %>
    <% end %>
  </div>
</div>

