<h1>Meus Dados de Saúde</h1>

<div class="row">
    <div class="col-sm-8 offset-sm-2">
      <div id="movies">
        <% @bases.each do |base| %>
          <h4><%= base.name %> <small>(<%= "#{base.description}" %>)</small> </h4>
        <% end %>
      </div>
    </div>
  </div>

<div class="container mt-5">
