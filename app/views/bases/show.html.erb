
<div class="container">

  <h2 class="mt-5 text-center mb-3"><%= @base.name %></h2>
  <%= link_to 'Voltar', :back, class: "btn btn-primary btn-ms" %>
  <%= link_to 'Solicitar meus dados', create_order_path(@base), class: "btn btn-primary btn-ms" %>
  <table class="table mt-3">
    <thead class="thead-dark bg-primary">
      <tr>
        <th scope="col">Variável</th>
        <th scope="col">Descrição</th>
        <th scope="col">Categoria</th>
      </tr>
    </thead>
    <tbody>
      <% @base.variables.each do |variable| %>
        <tr <%= "class=#{variable.category}" %> >
          <th scope="row"><%= variable.name %></th>
          <td><%=  variable.description %></td>
          <td><%=  variable.category %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
